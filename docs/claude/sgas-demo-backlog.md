---
name: sgas-demo-backlog
description: "SGAS post-demo backlog (two client meetings) + the Polish block that's been built"
metadata: 
  node_type: memory
  type: project
  originSessionId: 67e40e30-c1f5-4ddb-a890-9da0871fa266
---

=== UPDATE 27 JUN 2026 (read FIRST) ===
CALENDAR BUILT — a full in-app calendar (Month/Week/Day/Year; drag create/move/resize; holidays; engagements + members; admin-as-staff). It has its OWN memory: [[sgas-calendar]]. TEAMUP (item 9) is SCRATCHED — Chris decided not to use it; the calendar replaces it. teamup_api_key.txt + SGAS_Teamup_API_rationale.pdf are now moot (key never entered code; pushBlockToTeamup stub removed). So the ONLY remaining buildable backlog item is item 4 (staff quals + expiry tracking) — the next obvious build. New idempotent migrations Chris must run before testing the calendar live (in the Sgas project PARENT folder): sgas_user_staff_link, sgas_assess_nyc, sgas_holidays, sgas_engagements, sgas_engagement_members. Then commit+push.
=== END 27 JUN UPDATE ===

=== STATUS AS OF END 26 JUN 2026 (read this first) ===
BUILT THIS SESSION (all build-verified, synced to project files): Polish block (schedule chip colours+codes+clickable-ACS, preferred dates, dashboard month filter); dashboard declutter (collapsible cards + 5 stat tiles); item 11 (mixed new+reassessment per qual); item 13 (add-qualification to a booking — per-delegate blue "+" on Schedule Menus+Drag); item 2 (postcode lookup, postcodes.io) + item 3 (delegate address fields + copy-to-new-company); company logo (Chris's PNGs: sidebar=sgas-logo-white.png, login=sgas-logo.png, favicon=public/favicon.png a flame placeholder Chris may replace); item 1 (Inquiries tab); item 5 (Courses screen full management). SGAS_todo.html ticks: i1,i2,i3,i5,i6a-f,i11,i13 all done:true.
CHRIS MUST DO TO GO LIVE: (1) run TWO migrations in Supabase — `sgas_booking_cat_reassessment.sql` (item 11) + `sgas_inquiries.sql` (item 1); both also appended to `sgas_ALL_new_migrations.sql`. (2) GitHub Desktop commit+push ALL changes (Vercel auto-deploys sgas-opal.vercel.app — the URL Chris tests on). Other built items need NO migration.
STILL TO BUILD: item 4 Staff quals+expiry tracking (unblocked, next obvious). BLOCKED/WAITING-ON-CHRIS: item 7 pricing matrix (Chris maps the matrix → then quote-email in Inquiries), item 12 forms matrix (Chris sends off-tech-oil + QCF forms + course→form map), item 8 ACS alignment/overlay-vs-formfields decision, item 10 self-service booking (later).
TEAMUP (item 9) UPDATE (26 Jun 2026): Chris has now OBTAINED the Teamup API key — stored in `Sgas project/teamup_api_key.txt` (in the PARENT folder, OUTSIDE the sgas-app git repo so it won't be pushed; key value NOT in memory). I wrote `Sgas project/SGAS_Teamup_API_rationale.pdf` explaining why it's needed. STILL NEEDED before building pull/push: the account ADMIN LINK + the PER-SUB-CALENDAR access keys + the spare sub-calendars renamed "spare". CRITICAL BUILD NOTE: the Teamup key must be used SERVER-SIDE ONLY (e.g. a Supabase Edge Function the app calls) — NEVER in the Vite frontend, since Vite bakes env vars into the client bundle and would expose the key to every user. So the Teamup pull (course blocks by designator) + push (assignments to staff sub-calendars) replaces the pushBlockToTeamup STUB in api.js and needs a small backend, not just client code.
GOTCHA REMINDERS: (a) text inputs MUST have type="text" or they're unstyled in this app. (b) Mount-write gotcha persists — Edit/Write to Windows is correct but the bash MOUNT truncates overwrites; build-verify by replaying edits into /tmp/sgasv4 (or write full new files to outputs/ then cp). (c) leftover deletable files Chris can bin: preview_*.png, sgas-logo.svg/sgas-logo-white.svg (unused), the long pagespeed-named png, outputs/*.py/*.jsx scratch.
=== END STATUS ===

After the client DEMO walkthrough + a COURSES/PAPERWORK meeting (transcripts uploaded 26 Jun 2026), I wrote a 13-item backlog. Two deliverables live in the project root for Chris:
- `Sgas project/SGAS_demo_backlog.md` — full tagged list.
- `Sgas project/SGAS_todo.html` — clickable checklist (ticks saved in browser localStorage; tags NEW/POLISH/CHRIS/CODE/LATER — note: "CHRIS" = waiting-on-Chris, he asked to rename "YOU"→"CHRIS").

THE 13 ITEMS (grouped): New modules — Inquiries tab (lead capture under Dashboard, convert-to-delegate), postcode→address lookup (needs free source research), delegate↔company linking (address on delegate, copy-to-new-company for sole traders, add-to-existing), staff quals+expiry tracking+report, courses screen (stub), pricing matrix (Chris maps first). Polish — schedule chip colours/codes/clickable-ACS, preferred dates on board, dashboard blocks-by-month. Courses/paperwork meeting — (11) mixed new+reassessment on ONE booking [per-qual toggle cycle blue→green→off; currently mutually exclusive], (12) forms matrix: ACS gas form + off-tech-oil form + QCF form, each a different template, Chris to send forms + course→form map, (13) add-a-course to an existing booking (overlaps). Teamup+later — Teamup pull/push (needs API key + admin link + spare sub-calendars renamed), self-service booking link (later).

POLISH BLOCK BUILT 26 Jun 2026 (Chris picked "Polish on what's built" first; vite build PASSES 293 modules). Files changed: src/lib/api.js, src/views/Schedule.jsx, src/views/Dashboard.jsx, src/styles.css. Details:
- api.js listBlocks (live+demo): each block delegate now carries `kind` (NEW/REASSESS/NYC/NO_SHOW via new `delegateKind(disposition,isReassess)`) + `codes[]` (booking_category category codes). LIVE select gained is_reassessment,disposition,booking_category(category:category_id(code)) on the booking embed.
- Schedule.jsx: new shared `<DelegateChip>` (module-level `delForm(bookingId)` = getFormData→downloadForm; module-level `prefLabel`). Chip colours border+avatar by kind, shows codes after name, name is a `.dn-link` that prints THAT delegate's ACS form (stopPropagation so it doesn't trigger card click-to-place), kind tag for re/NYC/no-show. Replaced the plain delegate rows in BOTH BlockDelegates (Menus) and the Drag tab inline list. Preferred dates (📅 prefFrom–prefTo) now show on Menus pool rows AND Drag waiting chips.
- Dashboard.jsx: "Blocks awaiting assignment" card gained a Month <select> (client-side filter on awaitingBlocks by start YYYY-MM; only shows when >1 month present; tag shows "X of Y" when filtered). New state blockMonth + helpers monthKey/monthName/blockMonths/shownAwaiting.
- styles.css: +.delg .dn-link (dotted underline, brand on hover) + .dcodes.
NOT DONE in the Polish block: item 8 ACS form tweaks (tick/notch alignment nudge + contact-sheet + overlay-vs-form-fields decision) — left for Chris because it needs his eyes on the printed form + his "bomb proof" approach call.

MOUNT GOTCHA HIT AGAIN this session and is the reliable reproduction: Edit tool wrote api.js/Schedule.jsx/Dashboard.jsx CORRECTLY on the Windows side (Read tool tails show full content, longer than before) but the sandbox MOUNT kept the OLD byte length → tails truncated mid-token, and `git diff`/`wc -c` on the mount showed the pre-edit sizes. styles.css (smaller edit) synced fine. To BUILD-verify without trusting the mount: `git show HEAD:path > /tmp/copy` (HEAD was clean — confirmed via styles.css diff being exactly my 3 lines) then re-apply the SAME edits with a python str.replace script (asserting each old-string matches exactly once), then `npx vite build --outDir /tmp/x --emptyOutDir`. Worked first try. Chris commits from Windows/GitHub Desktop which reads the correct Windows files, so the truncated mount does NOT affect his deploy. See [[sgas-mount-write-gotcha]], [[sgas-frontend]], [[sgas-doc-generation]].

ACTION FOR CHRIS: GitHub Desktop commit+push (Vercel auto-deploys) — NO Supabase migration needed for the Polish block. Then tick items 6a–6e in SGAS_todo.html.

DASHBOARD DECLUTTER BUILT (26 Jun 2026, ad-hoc Chris request, build passes). Dashboard.jsx: every card is now a collapsible <DashCard> (start collapsed, header chevron toggles, a count badge shows even when collapsed). Open cards remembered in localStorage key `sgas_dash_open` (loadOpen()/toggleCard); fresh browser starts all collapsed. Top stat tiles moved from `.row.c3` to a responsive `.stat-row` (auto-fit minmax 150px); STAT_KEYS.ADMIN gained 'unassigned' + 'toAssess' (now 5 tiles: Expiring, Sessions, Blocks-awaiting, To-assess, Outstanding). styles.css: +.stat-row, .card.collapsible/.card-toggle/.chev/.card-count. Files: src/views/Dashboard.jsx, src/styles.css. NO Supabase. Dashboard.jsx was a FULL rewrite via Write tool → mount truncated; verified by writing full content to outputs/Dashboard_new.jsx (new files sync) then cp to /tmp build + cp over project. (The todo HTML now pre-ticks done items via a `done:true` flag + `isDone = (id in state)?state[id]:!!it.done` so localStorage still overrides.)

ITEM 11 — MIXED NEW+REASSESSMENT PER BOOKING BUILT (26 Jun 2026, build passes + node-verified). A single booking can now mix New and Reassessment per qualification. MIGRATION `Sgas project/sgas_booking_cat_reassessment.sql` (also appended to sgas_ALL_new_migrations.sql): `alter table booking_category add column if not exists is_reassessment boolean not null default false`. api.js: addToPool now takes cats with per-cat `kind`, builds `cat_kinds` map + entry-level `kind` SUMMARY (NEW/REASSESS/MIXED); new helper `catReassess(p,cid)`; bookingAttrs sets booking.is_reassessment = (kind REASSESS or MIXED) as a summary; scheduleCourse + addDelegatesToBlock booking_category inserts (LIVE + demo, both via replace_all) set is_reassessment: catReassess(p,cid); rescheduleDelegate preserves each re-booked category's is_reassessment (LIVE select gained is_reassessment); getPool returns catKinds. Book.jsx: REPLACED the whole-booking New/Reassessment seg toggle + checkbox grid with a per-qualification CYCLE — click a qual: off → Reassessment (blue, "Re") → New (green, "New") → off. State is `catKind` Map(category_id→'REASSESS'|'NEW') (was `checked` Set); createBooking passes per-cat kind; toast summarises New/Reassessment/Mixed. Schedule.jsx: KIND_COLOR/label/tag/DELEGATE_TYPES + drag legend gained MIXED (#7b2ff2 purple, tag 'mix'). demo.js: bc() gained `reassess=false` param (booking_category.is_reassessment); pool entry 103 (Owen, ACS Domestic) seeded kind:'MIXED' cat_kinds {1:REASSESS,2:NEW} so a purple MIXED chip shows in the waiting pool. styles.css: +.cat .catstate / .cat.k-re / .cat.k-new. Files: src/lib/api.js, src/lib/demo.js, src/views/Book.jsx, src/views/Schedule.jsx, src/styles.css (+ the SQL). ACTION FOR CHRIS: run the migration in Supabase, then commit+push. DESIGN CALLS made (easy to change): cycle order is Reassessment-FIRST (per transcript — reassessments are the common case); booking.is_reassessment kept as a summary; ACS form GN8 still uses that summary (per-delegate, GN8 rule still unconfirmed). NOTE block-delegate chip kind (delegateKind) is per-BOOKING so a mixed booking shows blue/REASSESS in the block — only the waiting POOL shows the MIXED colour.

ITEM 13 — ADD A QUALIFICATION TO AN EXISTING BOOKING BUILT (26 Jun 2026, build passes + node-verified). NO migration (reuses item-11's booking_category.is_reassessment). api.js: listBlocks delegates now also carry `categoryIds` (live+demo; LIVE select gained booking_category category_id); NEW `addQualsToBooking(bookingId, cats[{category_id,kind}])` inserts booking_category rows (PENDING, is_reassessment per kind), SKIPS quals already on the booking, and syncs booking.is_reassessment if any added is a reassessment (live+demo). Schedule.jsx (Menus tab only): new `<AddQualForm b categories onDone>` rendered under BlockDelegates in each expanded block — pick a delegate in the block + a qualification they don't yet hold (filtered to the block's scheme, or ALL cats when scheme unknown in live) + Re/New seg (reuses existing .seg .kind-re/.kind-new styles), Add → addQualsToBooking + reload. Loads listCategories in MenuAssign. styles.css: +.addqual/.addqual-row. Files: src/lib/api.js, src/views/Schedule.jsx, src/styles.css. INTERPRETATION (told Chris): "add a course to an existing booking" = add a QUALIFICATION to a delegate already on a block (the transcript's "little drop down… he's also going to do this"), same scheme, without re-booking — NOT scheduling onto a second course/block (that's still done via Book). REVISED UX (Chris, 26 Jun): the add-qual control is now a per-delegate blue "+" button on each DelegateChip (toggles to ×) that reveals an inline AddQualRow (qualification select + Re/New seg + Add) for THAT delegate only — no permanently-shown form. Present on BOTH Menus and Drag tabs (DelegateChip gets scheme/categories/onAdded props; BlockDelegates passes them; Drag passes them inline; AddQualRow replaced the old block-level AddQualForm). styles: +.delg-wrap/.addq-btn. Also deduped a doubled `.delg .dn-link/.dcodes` CSS block. Assess screen still has no add control. node-verified: add CKR1 to Tom's booking1 → cats [1,2]→[1,2,3], dupe re-add returns 0, per-cat + booking summary is_reassessment set. ACTION FOR CHRIS: just commit+push (no Supabase change).

ITEM 2 — POSTCODE LOOKUP BUILT (26 Jun 2026, build passes). Chris asked to reuse the "interpro" app's system — that app (folder C:\Users\chris\Desktop\interprowebsite, app.js) uses the FREE postcodes.io API (`https://api.postcodes.io/postcodes/{pc}`, NO key) for postcode→lat/lng+area. So SGAS now uses the SAME: NEW `src/lib/postcode.js` exports `lookupPostcode(pc)` → {postcode,town(=admin_district),county(=admin_county, often null for unitary so left blank),parish,region,lat,lng}. IMPORTANT LIMITATION (told Chris): postcodes.io is POSTCODE-LEVEL — validates + fills town/county only; it does NOT return a house-by-house address dropdown (the meeting's "pick your house number" needs a PAID PAF API e.g. getAddress.io/Ideal Postcodes — future upgrade). Book.jsx: new reusable `<PostcodeLookup value onChange onResolved>` component (postcode input + 🔍 Look up btn + ok/error/muted msg). Wired into BOTH forms: new-DELEGATE form gained structured address fields (House name/number=premise, Street, Town, County, Postcode) + lookup that fills town/county+normalised postcode — this also did the address-fields half of backlog item 3 (i3 now PARTIAL; copy-to-new-company + add-to-existing still TODO). new-COMPANY form (freeform `address`): lookup uses a transient nco._postcode and, on resolve, prefills the address field with "town, county, postcode" if empty. ITEM 3 COMPLETED (26 Jun 2026, Book.jsx only, build passes): added a `copyToCompany()` helper + "📋 Copy delegate → new company" button on the new-delegate form that opens the New company subform PRE-FILLED from the delegate (address joined from premise/street/town/county/postcode, contact=delegate name, phone=mobile, email) — sole-trader flow; user just adds the trading name then Save company (existing saveCompany sets nc.company_id). The company SELECT (relabeled "— add to existing company —") is the add-to-existing path. So i3 fully done.

LOGO ADDED (26 Jun 2026, build passes). Chris PASTED the SGAS logo image in chat (NOT saved as an uploaded file — uploads/ only had transcripts; find found no png), so I could SEE it but had no file bytes to bundle. Recreated it as a clean SVG: NEW `src/assets/sgas-logo.svg` (blue-gradient "SGAS" wordmark + water-drop/flame mark + "SPECIALIST GAS ASSESSMENT SERVICES" strapline, Arial Black) + NEW `public/favicon.svg` (just the drop). Wired: App.jsx imports logoUrl, sidebar `.brand` now shows <img className="brand-logo"> inside a WHITE rounded panel (so blue reads on the navy sidebar) + "Training Management" under it; Login.jsx shows <img className="login-logo"> (replaced the text brand-lg); index.html got <link rel=icon href=/favicon.svg>. styles.css: .brand restyled (text-align center, .brand-logo white chip), .login-logo (replaced .brand-lg). Vite import of .svg returns the URL string. Files: src/assets/sgas-logo.svg(NEW), public/favicon.svg(NEW), src/App.jsx, src/views/Login.jsx, src/styles.css, index.html. NO migration. UPDATE: Chris then saved the REAL logo file (transparent PNG, 300x150, blue 3D-bevelled "SGAS" + flame) into src/assets/ (originally named 300x100xmain-logo-nobg-300x150.png.pagespeed.ic.XsJTvnNb-_.png — a PageSpeed-optimised export). I copied it to clean name `src/assets/sgas-logo.png` and switched both imports (App.jsx + Login.jsx) from .svg → .png. So the app now uses the REAL logo. My `sgas-logo.svg` recreation is now UNUSED (Chris can delete it + the long pagespeed-named dup; I can't delete from sandbox — EPERM). favicon.svg (the drop mark) is still the browser-tab icon. Build passes with the PNG bundled.

FINAL LOGO STATE (26 Jun 2026): auto-tracing to SVG was a dead end (potrace kept adding edge slivers / breaking the end letters); Chris made his own in Photoshop. Now: App.jsx sidebar imports `src/assets/sgas-logo-white.png` (Chris's WHITE wordmark, clean on navy, NO white box), Login.jsx imports `src/assets/sgas-logo.png` (colour). index.html favicon → `/favicon.png` (a flame-A I cropped from the logo as a placeholder; Chris will replace public/favicon.png with his own later). My traced sgas-logo.svg/sgas-logo-white.svg + preview_*.png are now UNUSED (Chris can delete). Build passes. Imports are 1-line so swapping extensions is trivial if formats change.

ITEM 1 — INQUIRIES TAB BUILT (26 Jun 2026, build passes + node-verified). New lead-capture screen under Dashboard. MIGRATION `Sgas project/sgas_inquiries.sql` (also appended to sgas_ALL_new_migrations.sql): table `inquiry`(inquiry_id, name NOT NULL, email, mobile, courses text [comma-joined course NAMES], pref_date_from, pref_date_to, notes, status default 'open' ['open'|'converted'|'closed'], created_at, handled_at) + idx + permissive RLS/grants (same do-block pattern as renewal_contact). api.js: inqShape + listInquiries (open, newest first), createInquiry, setInquiryStatus(id,status) [live+demo]. demo.js: store.inquiries seeded (Dave Stott, Sue) + seq.inquiry=2. NEW `src/views/Inquiries.jsx`: 2-col — left = quick-capture form (Name required; at least one of Email/Mobile; course tick-boxes from listCourses by course NAME; optional pref from/to; notes), right = open list (name+ago, contact, wants/pref/notes, Convert + Close). Convert calls setInquiryStatus('converted') then go('book',{name,email,mobile}); Close → setInquiryStatus('closed'). roles.js: 'inquiries' added to ADMIN/STANDARD/SCHEDULER (after dash). App.jsx: import Inquiries, NAV_GROUPS Operations gained inquiries (💬) after dash, TITLES.inquiries, render `<Inquiries go={go}/>`. CONVERT PREFILL: App.jsx `go` rewritten to route its 2nd param — `setOpenDelegate(v==='delegates'?param:null); setBookPrefill(v==='book'?param:null)` + new bookPrefill state + `<Book prefill={bookPrefill}/>`. Book.jsx: new `prefill` prop + useEffect splits name→forename/surname, sets email/mobile, opens the new-delegate subform. Files: src/lib/api.js, src/lib/demo.js, src/lib/roles.js, src/App.jsx, src/views/Book.jsx, src/views/Inquiries.jsx (NEW) + the SQL. ACTION FOR CHRIS: run sgas_inquiries.sql in Supabase, then commit+push. DEFERRED: quote-email with pricing (needs pricing matrix item 7).

INQUIRIES CSS FIX (26 Jun 2026): Chris said the Name/Email/Mobile fields + course boxes looked "off / square". ROOT CAUSE: the app only styles `input[type=text]` (styles.css L65) — my bare `<input>` (no type attr) didn't match → fell back to unstyled narrow browser boxes. FIX: added type="text" to the 3 inputs; also replaced the boxy `.cat` course grid with compact pill chips (.inq-courses/.inq-chip in styles.css). LESSON: ALWAYS put type="text" on text inputs in this app or they're unstyled. (Verified the page live via a screenshot of sgas-opal.vercel.app — the deployed Vercel URL is what Chris tests, so changes need commit+push to show.)

ITEM 5 — COURSES SCREEN BUILT (26 Jun 2026, build passes 297 modules + node-verified). NO migration (course/category tables already exist). api.js: createCourse/updateCourse (name/scheme/price/teamup_designator/is_active) + createCategory/updateCategory (code/description/scheme/renewal_years) — live + demo. demo.js seq gained course:10, cat:38 (categories seeded 1-38, courses 1-10, no counters before). Courses.jsx REWRITTEN from the read-only stub into full management: ＋ Add course form; each course is a collapsible `.cgroup` card (header shows name + scheme + £price + inactive badge + "N quals · N sessions"); expand → <CourseEdit> (name/price/teamup designator/Active checkbox) + <QualSection> (the categories in that course's SCHEME — add via <AddQual>, inline-edit each via <QualRow> editing code/description/renewal years). styles.css: +.addqual-row input{flex:1}. Files: src/lib/api.js, src/lib/demo.js, src/styles.css, src/views/Courses.jsx. MOUNT GOTCHA: Courses.jsx was an OVERWRITE of the existing stub so the bash-mount copy truncated at L21 → cp to /tmp gave a truncated file → vite "Expected > but found EOF". FIX: wrote the full content to a NEW file in outputs/ (new files sync clean), cp'd that to /tmp AND over the project path. ACTION FOR CHRIS: commit+push (no Supabase change).

api.js createClient LIVE insert now persists premise/street/town/county/postcode (demo already spread ...d; client table already has these columns, feeds the ACS form's house/street/town/county/postcode). styles.css: +.pc-msg(.ok/.error/.muted). Files: src/lib/postcode.js (NEW), src/views/Book.jsx, src/lib/api.js, src/styles.css. NO Supabase migration. Did NOT live-test postcodes.io from sandbox (web-fetch guidance) — field mapping mirrors interpro's confirmed usage (postcode/latitude/longitude/parish/admin_district). ACTION FOR CHRIS: commit+push.
